# 未決定事項 — 実装しないこと

**勝手に決めて実装せず、口だけ作ってスタブに留めること。**

## Q-F1 既存の推薦表示（`CheckInRecommendView`）をどうするか

現在、チェックイン成功後に推薦ブースを3件出す画面がある（`src/features/checkin/pages/CheckInRecommendView.tsx`、`GET /recommendations` を叩く）。

段階解放では**推薦はカードの外側12マスに同化させる**方針であり、「推薦欄」のような外部 UI は企画上許されていない。この画面をどうするかは未決定。

候補: (a) 削除する / (b) 解放前だけ残す（カードに推薦が無い期間の動線として）/ (c) 「次はこのマスを狙おう」というカード内マスへの誘導に作り替える

→ **未決定。既存の実装をそのまま残し、触らないこと。**判断が出るまで挙動を変えない。

## Q-F2 評価の段階数（5 or 3）

**サーバーの `rating_scale` に従う実装にする**ことで、フロント側は決定を待たずに実装できる。**星の数をハードコードしないこと**だけが要件である。既定は3。

## Q-F3 理由文の文言と生成ロジック

**未決定**（サーバー Q-3）。表示コンポーネントとデータ受け渡し口だけ作り、`reason === null` のあいだは理由セクションを出さない。**フロント側で仮の文言を生成しないこと。**

## Q-F4 事前アンケートの設問

既存の `src/features/presurvey/` を**そのまま使う**。設問構造が変わるまで触らない（サーバー Q-2）。

## Q-F5 解放演出の具体的なビジュアル

「外側12マスが開くことが分かる」以外は未決定。デザインの詳細は実装者判断でよいが、**凝りすぎないこと**。当日 13:30–15:30 に集中して発火するため、重い演出は避ける。

---

**上記のいずれかを実装しようとしていることに気づいたら、手を止めて確認すること。**
