# テスト実行記録 — 2026-05-13（モック認証）

## 何を

- `tests/unit/auth-mock.test.ts` を追加（`mockLogin` の成功・失敗）。
- 既存 `tests/unit/api-unwrap.test.ts` と合わせて `npm test`。

## なぜ

バックエンドなしでログイン UI を切り離すため、モックの挙動を Vitest で固定する。

## 実行コマンド

```bash
cd tests && npm test
```

## 結果

- Vitest 3.2.4: テストファイル 3、テスト 5 件すべて成功（`auth-mock` 含む）。
- `frontend`: `npm run lint` / `npm run build` 成功。