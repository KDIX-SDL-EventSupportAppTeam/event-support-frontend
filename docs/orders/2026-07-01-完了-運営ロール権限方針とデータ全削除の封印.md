# 作業指示: 運営ロール権限方針の確定とイベントデータ全削除の封印 — フロント側

- **日付:** 2026-07-01
- **状態:** 完了
- **関連:** 同名 server order / `2026-07-01-完了-変更履歴（監査ログ）admin表示.md`

## 背景

サーバー側で「イベントデータ全削除」を封印（無効化）する決定に伴い、
admin 画面から該当パネルを撤去する。manager の他の操作権限は変更なし
（既に全 write 操作が可能）。

## 決定事項（ユーザー確認済み）

- manager は全 write 操作可（変更不要）。viewer は参照のみ。
- スタッフ管理は主催者のみ（変更不要）。
- **イベントデータ全削除は封印** → admin UI から撤去。トリガー UI は当面設けない。

## 作業内容

- [x] `src/features/admin/pages/SampleDataPage.tsx`
  - `EventDataClearPanel` のインポート・設置を削除。説明文を「サンプルデータの生成・削除」に修正。
- [x] `src/features/admin/components/EventDataClearPanel.tsx` を削除（孤立コード除去）。
- [x] `src/shared/api/v1Admin.ts`
  - `clearAllAdminEventData` と `EventDataClearResult` を削除。
- [x] サンプルデータの生成・削除（`SampleDataPanel`）は manager 操作として存置。

## 受け入れ条件

- `/admin/sample` に全削除パネルが表示されない。
- サンプルデータ生成・削除は従来どおり動作する。
- `npx tsc --noEmit` がクリーン。
- 全削除関連の残存参照なし（grep 済み）。

## 実装メモ

- 補足: フロントの `isManagerUser`（authStore）は未使用のままで、viewer にも
  write ボタンが表示される（押下時サーバーが 403）。viewer は問題なしとの方針だが、
  UI で隠す制御を入れるかは任意（別タスク候補）。

## 関連ドキュメント

- AGENTS.md（feature 構成・データ層）
