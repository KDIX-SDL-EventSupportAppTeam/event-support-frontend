# ルート `tests/` について

Vitest でフロント（`../frontend/src`）のロジックを `@/` エイリアス経由で検証する。

## コマンド

```bash
cd tests && npm install && npm test
```

フロントのビルド確認:

```bash
cd frontend && npm install && npm run build
```
